import { Component } from '@angular/core';

@Component({
  selector: 'app-create-invite',
  template: `<app-form-invite></app-form-invite>`
})
export class CreateInviteComponent {
  constructor() {}
}
